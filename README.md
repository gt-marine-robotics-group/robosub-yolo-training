# robosub-yolo-training

Training pipeline for RoboSub YOLO models.

## Setup

- Create venv

```bash
python3 -m venv .venv
````

* Activate it

```bash
source .venv/bin/activate
```

* Install dependencies

```bash
pip install -r requirements.txt
```

* Run training

```bash
python train.py
```

